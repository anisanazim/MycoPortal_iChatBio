# Tool: list_morphology

Use when user asks about morphological characters/traits tracked by MycoPortal,
or asks for possible states/values/options for morphology traits.

Endpoint: GET /api/v2/morphology

Key params:
- includeStates (0|1)
- limit
- offset
